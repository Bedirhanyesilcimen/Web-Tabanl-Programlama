@model WorkFlowApp.Models.GenelUretimViewModel

@{
    ViewData["Title"] = "Genel Üretim Takip";
}

<div class="container-fluid">
    <h1 class="h3 mb-4 text-gray-800">🏭 Genel Üretim Paneli (Yönetici)</h1>

    <div class="row">
        <div class="col-lg-6">
            <div class="card shadow mb-4">
                <div class="card-header py-3 bg-info text-white">
                    <h6 class="m-0 font-weight-bold">Kaplama Bölümü (Son İşlemler)</h6>
                </div>
                <div class="card-body">
                    <div class="table-responsive">
                        <table class="table table-sm table-bordered">
                            <thead>
                                <tr>
                                    <th>Tarih</th>
                                    <th>Ürün</th>
                                    <th>Operatör</th>
                                </tr>
                            </thead>
                            <tbody>
                                @foreach (var item in Model.KaplamaListesi)
                                {
                                    <tr>
                                        <td>@item.Tarih.ToShortDateString()</td>
                                        <td>@item.UrunAdi</td>
                                        <td>@item.Operator</td>
                                    </tr>
                                }
                            </tbody>
                        </table>
                        <a href="/Kaplama/Index" class="btn btn-sm btn-info w-100">Bölüme Git & Detay Gör</a>
                    </div>
                </div>
            </div>
        </div>

        <div class="col-lg-6">
            <div class="card shadow mb-4">
                <div class="card-header py-3 bg-warning text-dark">
                    <h6 class="m-0 font-weight-bold">Tekstil Bölümü (Son İşlemler)</h6>
                </div>
                <div class="card-body">
                    <div class="table-responsive">
                        <table class="table table-sm table-bordered">
                            <thead>
                                <tr>
                                    <th>Tarih</th>
                                    <th>Makine</th>
                                    <th>Personel</th>
                                </tr>
                            </thead>
                            <tbody>
                                @foreach (var item in Model.TekstilListesi)
                                {
                                    <tr>
                                        <td>@item.Tarih.ToShortDateString()</td>
                                        <td>@item.MakineAdi</td>
                                        <td>@item.Personel</td>
                                    </tr>
                                }
                            </tbody>
                        </table>
                        <a href="/Tekstil/Index" class="btn btn-sm btn-warning w-100">Bölüme Git & Detay Gör</a>
                    </div>
                </div>
            </div>
        </div>
    </div>
</div>